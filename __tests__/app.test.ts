import request from 'supertest';

// import app from '../src/app';

describe('/user', () => {
  it('/user/register endpoint returns user', (done) => {
    // return request(app)
    //   .post('/api/user/signup')
    //   .send({username: 'tester', password: 'password'})
    //   .set('Accept', 'application/json')
    //   .expect(201, done);
  });
});
